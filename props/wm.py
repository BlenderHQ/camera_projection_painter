from __future__ import annotations
_B='import'
_A='WMProps'
from..import constants
from..import icons
from bpy.types import PropertyGroup
from bpy.props import BoolProperty,EnumProperty
__all__=_A,
class WMProps(PropertyGroup):setup_context_stage:EnumProperty(items=((constants.SetupStage.PASS_THROUGH.name,'Pass-Through','',0,constants.SetupStage.PASS_THROUGH.value),(constants.SetupStage.INVOKED.name,'Invoked','',0,constants.SetupStage.INVOKED.value),(constants.SetupStage.PRE_CLEANUP.name,'Pre-Cleanup','',icons.get_id('cleanup_small'),constants.SetupStage.PRE_CLEANUP.value),(constants.SetupStage.IMPORT_SCENE.name,'Import Scene','',icons.get_id(_B),constants.SetupStage.IMPORT_SCENE.value),(constants.SetupStage.CHECK_CANVAS.name,'Check Canvas','',icons.get_id('image'),constants.SetupStage.CHECK_CANVAS.value),(constants.SetupStage.CHECK_CAMERAS.name,'Check Cameras','',icons.get_id(_B),constants.SetupStage.CHECK_CAMERAS.value),(constants.SetupStage.CHECK_IMAGES.name,'Check Images','',icons.get_id('bind'),constants.SetupStage.CHECK_IMAGES.value),(constants.SetupStage.CHECK_TOOL.name,'Check Tool','',0,constants.SetupStage.CHECK_TOOL.value)),options={'HIDDEN','SKIP_SAVE'},default=constants.SetupStage.PASS_THROUGH.name);configure_udim:BoolProperty(default=True,translation_context=_A,name='Configure UDIM',description='The add-on uses some standard methods for importing, but in certain situations this creates incorrect data for the add-on to work with. If this option is activated, after importing the object, the materials for working with UDIM textures will be set, and after selecting the canvas, the image settings will be checked and adjusted')